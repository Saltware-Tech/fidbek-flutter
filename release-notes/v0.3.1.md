# fidbek_flutter v0.3.1

## Summary

Patch release for iOS wrapper packaging cleanup.

## Changes

- Removed stale `CoreMotion` linkage from `ios/fidbek_flutter.podspec`.
- Fixes iOS installs that could fail with `missing required module 'CoreMotion'`.
- Bundled native SDK artifacts remain on native `0.3.0`.
